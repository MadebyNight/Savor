# 食光项目开发规范

## 项目目标

- 产品形态是 Android 本地应用，不再把网页版作为交付目标。
- 使用 Capacitor 将现有 React + Vite 前端封装为 APK。
- 核心流程必须支持离线运行：菜谱、点单、采购清单、周菜单和冰箱库存。
- 首版面向个人使用，不实现账号体系、多人实时协作或后台常驻同步。

## 已确认业务规则

- 采购清单按“选菜确认份数”计算；周菜单中的重复安排不增加采购份数。
- 一份代表一道完整菜，不支持用餐人数和个人摄入换算。
- 菜谱支持新建、编辑和删除；删除不能破坏历史菜单中的菜谱快照。
- 周菜单按真实日期保存，支持将历史菜单复制到指定周后独立修改。
- AI 优先支持 DeepSeek；已核验当前官方模型 deepseek-flash 支持图像与 Chat Completions，实际能力以接入时官方文档为准。
- 小红书链接优先评估复用开源方案，避免重造爬虫；不要求官方，但应适合 Android 本地运行。获取后先生成可编辑草稿；暂不适用时保留粘贴正文、截图、手动录入。

- 首版不包含热量录入、计算、标签、每日目标或提醒。

## 技术约束

- Android 工程位于 `android/`，使用 Capacitor 管理，不手改生成的前端资源。
- 前端改动后按顺序执行 `npm run build`、`npx cap sync android`，再验证 Android 构建。
- 结构化数据使用 SQLite；图片使用应用私有文件目录；普通偏好使用 Preferences。
- API Key 和坚果云 WebDAV 应用密码使用 Android Keystore 管理的密钥加密保存。
- 不把密钥写入仓库、APK 固定配置、日志或同步包。
- 坚果云首版只做用户主动触发的 WebDAV 同步；离线修改必须保留，冲突不能静默覆盖。
- AI 请求由 Android 应用直接发出，推理由服务商云端完成；发送图片或文本前应给出明确提示。

## 开发流程

1. 修改前先检查 Git 状态、相关文档和现有实现。
2. 复杂改动先列出目标文件和 3–6 条实施计划。
3. 优先做小范围、可审查的增量，不覆盖无关改动。
4. 行为变更优先补充或更新有价值的验证；不为简单静态改动堆测试。
5. 修改后至少运行最快的相关构建、语法检查或人工抽查，并说明未验证部分。
   - 所有测试先开启日志：命令测试保留输出，连接手机测试前运行 `scripts/android-logcat.ps1 -Serial <设备序列号>`。日志保存在忽略目录 `.android-tools/device-logs/`，不得记录 Key、密码、请求头或业务正文。交给用户手动测试时保留采集并告知路径；USB 断开、电脑休眠或采集进程退出后，重新确认并恢复采集。
6. 删除文件或目录前列出对象、原因和影响，并取得二次确认。

## Git 与文件

- 不提交 `.android-tools/`、`node_modules/`、`dist/`、密钥、API Key、WebDAV 密码和本地配置。
- 保留用户已有改动，不重置、不覆盖无关文件。
- 提交前运行 `git diff --check`，只提交本次相关改动。
- 提交信息使用中文，Conventional Commits 类型保留英文，例如 `feat: 初始化 Android 工程`。

## 文档同步

- 产品规则变更同步更新 `docs/产品需求确认.md` 和 `docs/开发规划.md`。
- 技术实现与规划不一致时，先更新文档或明确记录偏差，再继续扩展实现。
- README 中的启动、构建和 Android 操作说明必须与实际脚本保持一致。
