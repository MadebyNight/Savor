# 食光用户体验与美学优化整合评估

整理日期：2026-09-23。代码核对基线：`5f30c28`（统一紧凑页面布局并整合菜谱添加入口）。

本文合并[用户体验与功能评估](用户体验与功能评估-2026-09-22.md)和[美学检查](美学检查.md)，是后续优化讨论的统一入口。两份原报告保留历史证据；其问题状态、建议优先级及冲突处理以本文为准。

**性质：评审结论与建议方案，不是 UI 已改版、功能已修复或新需求已批准的声明。** 本次仅整理文档，没有修改程序、重新运行手机验收或调用外部服务。

## 1. 整合结论

两份评审的共同方向是：保留米白、暖黄、灰绿和系统字体，让菜品、食材、当天安排成为视觉中心，减少工具和重复说明抢占注意力，同时缩短高频任务的操作路径。

不能直接拼接两份待办：2026-09-22 的评估之后，采购勾选、冰箱推荐、短状态标签、导入合并和多页密度已经更新。后续工作应以现有成果为基础，而不是再次重做这些功能。

统一处理原则：

1. 已确认需求约束现有行为；评审可以提出新方案，但不能将建议直接记成已批准需求。
2. 当前代码确定功能是否存在；已有测试与截图说明当时验证范围，不能代替本轮交互验收。
3. 数据不丢失、计算口径和关键状态可理解性，优先于装饰性调整。
4. 同一页面的链路、信息层级和视觉样式合并设计；数据算法和存储修复独立验证。
5. 保留已达到的紧凑度，不再全面压缩行高，也不通过缩小字体、触摸区域换取空间。

## 2. 重新核对后的现状

### 2.1 已处理或已有明显进展的旧问题

| 原结论 | 当前核对结果 | 整合处理 |
| --- | --- | --- |
| 采购不能勾选已买 | App.jsx 已有勾选；domain.js 按名称、单位、数量核对勾选有效性；开发进度记录重启和数量变化验证 | 从功能缺失清单移除；保留完成反馈的视觉优化 |
| 冰箱先选食材再跳点单找菜，候选含过期批次 | 当前由独立推荐面板匹配整个菜谱库，排除过期批次，支持一组明确同义名称 | 旧算法批评不再适用；保留推荐入口位置和“已有种类不等于数量齐全”的表达问题 |
| 冰箱首条库存接近半屏，长风险标签挤压名称 | 已合并搜索与期限筛选、去掉宣传语独占行、批次数并入标题，状态简化 | 不重复要求压缩顶部；继续优化工具重量和未知期限状态 |
| 点单首屏只能看到约 3 道菜 | 开发进度和新截图记录常规样本完整显示 5 道，48px 加减点击区保留 | 不再以旧截图要求全面压缩；转向菜名、图片、加号的主次 |
| 菜谱导入要先选择图文或正文 | 已直接进入统一导入页，图片、链接和正文同页；另有手动添加 | 不再新增统一入口；只整理素材区、操作权重和草稿保护 |
| 菜谱库缺少最新独立实屏 | 找到并查看 `library-final-20260923/recipe-library.png`；新列表首屏呈现 8 道样本菜谱 | 美学报告的证据缺口已补，保留紧凑列表，先改善封面质量和无图辨识度 |
| 设置展示实际请求地址等冗余说明 | 当前 SettingsPanel 已精简部分技术说明；横向分页仍在 | 移除过时文案指摘；继续优化设置导航和字段组织 |

“已处理”仅指该项原问题已被当前实现和已有记录覆盖，不代表相关模块全量验收。

### 2.2 仍存在或仍需验证的事项

- **代码仍存在：** 切换编辑对象覆盖单份草稿、采购不换算 g/kg、成功提示早于异步持久化、餐次只能选已确认菜品、菜单缺少直接查看快照做法入口、保存方式与期限重算耦合、日期逐月翻页、回顾中 AI 生成区前置。
- **功能扩展候选：** 主动入库、快捷消耗、直接填写到期日、营养重复输入复用。这些不是全部已确认需求中的缺陷。
- **待验证风险：** 单张图片读取失败阻断整体加载、全量保存和图片缓存的规模表现。
- **已有失败记录、尚未闭合：** OPPO 后台定时提醒延迟。界面美化不能视为解决该问题。

## 3. 可以一起优化的八组工作

各组中的“统一建议”是评审推荐方向。涉及已确认设计改变的部分见第 4 节裁决，实际开发前需更新相应设计或需求记录。

### O-01 点单：选择状态与操作视觉一起调整

**合并来源：** 功能评估 F-01、F-02、页面视觉评估；美学检查第 3 节。

- 保留左分类和紧凑小图单列，先让菜名突出，食材摘要与分类数量降低权重。
- 未选菜的加号使用浅底或轻描边；选中后突出数量和操作反馈，48px 点击区不缩小。
- 图片统一裁切和品质；无图时优先使用已有图标体系内的分类占位，避免让占位图比真图更抢眼。
- 将“加入菜单”统一为“加入待选”，“确认我的菜单”改为能反映实际动作的“确认选菜”；成功提示不暗示已经排好日期与餐次。
- 底栏必须区分：无待选且无当前采购；有待选；待选与当前采购不同。零待选但有当前采购时保留查看和明确清空入口，不能直接隐藏成无法操作。
- 用简短状态名称区分待选与当前采购，详细差异按需查看，不恢复大段常驻解释。

**验收：** 零待选但存在采购时不误导；确认空清单仍需明确影响确认；加减和确认区触摸面积不退化；长菜名不截断；同样样本、视口和字号下不明显倒退现有浏览效率。

### O-02 周菜单：阅读顺序、做法入口与餐次管理一起设计

**合并来源：** UX-01、UX-02、UX-06、UX-08、页面视觉评估；美学检查第 4 节。

- 推荐阅读顺序：目标周与日期 → 当天五餐 → 当日预计营养 → 本周回顾；历史与清空保持可见的同排工具，不增强清空的视觉权重。
- 保留“一饭一饮 三餐四季”为轻量题签，不再占大块独立空间。
- 餐次名称作为分组，菜名作为主信息；空餐次用轻提示和小加号，整行可操作。
- 菜名提供查看当时菜谱快照的入口，与份数调整、移除区分；已删除原菜谱也应能看历史做法。
- 点击某餐次后保留目标，不让重复的餐次选择控件占据主要空间；顶部安排入口明确目标餐次。
- 历史优先列出有菜单的周，日期控件补快速月份定位。
- 餐次直接搜索全菜谱列为独立流程变更候选；在确认前仍使用已确认菜品池，不能借视觉改版暗中改变采购或排餐来源。

**验收：** 空五餐和完整五餐都清楚；历史快照可读；返回保持日期；不随重复安排增加采购量；不能在整行按钮中嵌套另一个按钮来实现做法入口。

### O-03 营养回顾：已有结果、补缺和 AI 建议统一分层

**合并来源：** F-08、页面视觉及复杂弹窗评估；美学检查第 7 节。

- 推荐结构：目标周与简短概况 → 每日切换及已有预计营养 → 本周合计 → 下周建议 → 高级计算入口。
- 无数据时使用紧凑空状态；部分估算保留状态，未知仍为“—”，不替换成 0。
- 不在普通页面恢复逐食材缺失清单、来源和覆盖项数；这些仍在高级计算中查看。补缺汇总也在高级计算内组织。
- 已有建议允许离线阅读；没有建议时采用轻量生成入口，避免空的大型 AI 卡片占满首屏。
- 推荐回顾采用独立全屏二级页面，支持持续阅读；高级计算作为次级视图，返回保留日期。无需将所有短选择弹窗改成页面。
- 相同输入营养结果复用先限定明确相同的食材、数量、单位和计算版本；更新历史快照必须主动选择。

**验收：** 空周、部分估算、完整数据、已有建议、建议过时、离线和失败均可理解；7 天切换可达；不加入个人健康目标或实际摄入结论；AI 发送确认及迟到结果保护保持。

### O-04 冰箱：维护动作与风险视觉一起精修

**合并来源：** UX-04、UX-05、F-04 至 F-07、页面视觉评估；美学检查第 5 节。

- 沿用已完成的搜索/期限组合框、横向分类、拍摄/相册/手动三入口，三入口视觉统一为轻工具栏，不折叠成新的多步菜单。
- 行内名称与期限优先，数量第二，分类弱化；保持过期、临期的文字及语义色，减少背景、侧边、标签三重强强调。
- 未知期限可显示紧凑“期限待补”，完整含义在编辑页可见；不能暗示未知就安全。
- 推荐算法已更新；本轮仅建议让“看看能做什么”不被长列表埋没，并继续显示“已有 N 种”，不称“食材已齐”。
- 快捷消耗/已用完、直接到期日输入、修改方式但保留期限分别作为维护能力候选；不因排餐而自动扣库存。

**验收：** 不再退回旧长状态标签；长食材名和未知期限不互相挤压；过期批次不推荐和抵扣；三种添加可直接到达；数量、日期、保存期规则保持一致。

### O-05 菜篮子：已买进度与清单视觉一起完善

**合并来源：** UX-03、F-03、页面视觉评估；美学检查第 6 节。

- 复用现有勾选，不新增另一份购买状态；顶部展示已买/总计或待买数量，进度条不是必需项。
- 去掉每行重复的红色“待买”，分类使用中性辅助色；名称、数量与单位对齐，已买行降低权重但仍可取消勾选。
- 已买不等于已入库。视觉进度不能改变采购计算、库存数量或未来清单。
- 当前保留左分类；如后续试验顶部筛选，限于菜篮子，不顺带改点单。未确认前不作为必做改版。
- 主动“登记入库”作为独立能力候选，须让用户确认批次、数量、单位及日期；不将勾选绑定自动入库。

**验收：** 勾选重启保留；需求数量变化后失效；条目移除清理；可取消勾选；未知数量不变成 0；总计口径使用清单条目，避免混合单位误报食材种类数。

### O-06 菜谱库、详情、导入与编辑：内容层级和草稿安全一起处理

**合并来源：** FE-01、详情及编辑页视觉评估；美学检查第 3、8 节。

- 最新菜谱库已是紧凑列表，保留已确认密度，不默认改成大封面卡片或瀑布流。收藏感通过更好的封面裁切、分类占位、完整菜名和整齐元信息获得。
- 菜谱详情优先食材与步骤，图片适度收紧，编辑/删除收进次级管理区；不与菜谱库封面目标混为一谈。
- 导入沿用统一素材区，图片入口、缩略图与链接/正文有清晰关联；输入区适度随内容增长，保存草稿次于识别/核对保存。
- 保留最新的“导入菜谱 → 手动添加”入口与草稿延续；入口名称未来可讨论是否改为更涵盖手动录入的“添加菜谱”，本轮不恢复已取消的独立新建按钮。
- 手动表单优先菜名和食材，图片可选，正式保存按钮稳定可达。
- 切换编辑菜谱前保护原草稿；只减弱“保存草稿”按钮样式，不删除持久化、错误提示或放弃确认。

**验收：** 相机/相册取消保留素材；网络失败可手动继续；编辑 A 再编辑 B 不静默丢 A；浏览历史做法不写回原菜谱；长列表、大字体、键盘下保存入口可达。

### O-07 设置与提醒：导航层级与状态反馈一起优化

**合并来源：** UX-07、F-09、设置视觉评估；美学检查第 8 节。

- 推荐设置首页分组列出 AI、备份恢复、坚果云和提醒，点击进入配置，避免长标题横向分页隐藏功能。
- 二元启停可统一开关样式，星期/时间使用紧凑设置行；不改变已有开关语义及保存时机。
- 保留必要字段名、错误和实际权限状态，不恢复已移除的实际请求地址等技术说明。
- 已启用提醒、已获通知权限、已设置调度、实际按时投递不是同一状态，不能用一个漂亮的“已开启”掩盖可靠性缺口。
- 从识别进入 AI 设置后仍返回原任务；从回顾进入提醒设置后返回对应目标周。

**验收：** 所有设置功能容易发现；返回不丢输入；权限拒绝和撤销状态真实；没有重复权限请求；后台定时另做真机验收，不计入视觉改版通过项。

### O-08 共用视觉规则与前端可靠性

**合并来源：** 美学检查第 2 节及各页共性；功能评估 FE-03 至 FE-05。

- 实黄集中于主提交和必要状态，选中用浅黄；普通操作中性化。库存临期的语义黄色是独立用途，不因“黄色收紧”而取消。
- 名称、数量、状态靠字重、间距与对齐建立层级，不把所有辅助文字继续调淡。
- 搜索框与普通输入框只保留轻边界和轻阴影，减少叠加强调；沿用既有配色与可见焦点，不另造一套色板。
- 保留系统字体、安全区、减少动画设置；图形可小，既有主要操作 48px 点击区不缩。
- 正式保存成功以持久化完成为准；单图片失败应允许文字数据降级显示。全量保存性能先测量，再决定是否优化。

**验收：** 对比度、焦点、TalkBack、长文本、禁用态需要专门检查；不能从截图美观推定无障碍合格。图片故障和存储失败采用隔离数据测试。

## 4. 冲突与取舍裁决

以下给出单一处理结论，不把相反建议同时交给开发执行。“建议采用”不等于覆写现有已确认规格。

| 编号 | 冲突或表面差异 | 统一结论及理由 |
| --- | --- | --- |
| C-01 | 旧功能报告说没有采购勾选，美学报告已有复选框 | 版本差异。按当前实现认定已具备勾选；仅优化反馈和样式，不重做状态模型 |
| C-02 | 旧报告要求更紧凑，美学建议不要继续压缩 | 紧凑度已经有新成果。后续重排视觉权重，不全面减行高；长文和大字号允许更高条目 |
| C-03 | 美学要求食物图片更突出，最新需求要求小图紧凑列表 | 保留紧凑列表，通过质量、裁切和占位图提升辨识度；不默认放大封面。详情页仍应压低大图对做法的遮挡，两种页面目标不同 |
| C-04 | 加号弱化、零份底栏降权，与选菜效率和清空能力冲突 | 弱化视觉，不缩点击区；零待选但有已确认采购时保留清空路径和确认，不能按零份直接隐藏 |
| C-05 | 两份评审都建议营养数据在前，但 2026-09-22 已确认规格强调下周建议 | 整合推荐选择已有数据优先，理由是离线价值和真实内容优先；明确这是设计变更候选。批准并同步规格前，不能把当前 AI 前置称作实现错误 |
| C-06 | 原报告先修功能，美学报告优先周菜单和回顾 | 分开排序：草稿丢失等正确性问题最高；纯 UI 工作先周菜单/回顾，再点单、设置。后台提醒可靠性独立安排，不以美化代替，也不阻塞全部视觉设计 |
| C-07 | 推荐顶部分类与已确认左栏分类冲突 | 点单保留左栏；冰箱沿用已确认横排；菜篮子顶部筛选仅作后续对比候选，避免一次改三个页面导航 |
| C-08 | 解释选菜/采购差异与最新“精简页面说明”冲突 | 用准确按钮名、简短状态和按需详情解决，不恢复说明段落；错误、缺失及删除/覆盖确认必须保留 |
| C-09 | 更完整购物流程与最新“勾选仅记进度、不自动入库”冲突 | 保留勾选语义，入库只能是单独主动确认的新能力；不能为减少一步而把两者绑定 |
| C-10 | g/kg 换算被旧报告列为高优先级修复，但需求明确不做比例换算 | 降为高价值能力扩展候选，不称违反规格的 bug，不与 UI 改版捆绑。批准后仅做可靠同量纲换算，更新测试及勾选失效口径 |
| C-11 | 全菜谱直接排餐与已确认菜品池规则冲突 | 保留当前来源；直接搜索全库列独立流程提案。无论采用哪种来源，安排都不能暗中增加采购量 |
| C-12 | 美学希望减少风险色，与过期/临期识别冲突 | 减少背景、侧边和标签的重复强强调，保留状态文字和至少一种明确语义色，不将风险行做成普通行 |
| C-13 | 全屏回顾/分组设置与已有弹窗/横向分页规格冲突 | 推荐回顾独立二级页、设置分组；短选择和餐次快速编辑继续弹窗。作为后续设计变更记录，返回和草稿行为不变 |
| C-14 | 修改保存方式但保留期限，与现有“修改方式先提示重算”冲突 | 作为候选细化方案：允许保留现值，重算仍明确确认；不移除包装优先规则。批准前不擅自改期限计算 |
| C-15 | 美学报告尚无菜谱库实屏，且之后又发生入口调整 | 已查看新截图，评估基线更新为 5f30c28；保留现有统一添加路径，不根据旧截图恢复新建入口 |

这些裁决消除了执行方向冲突；涉及产品选择的候选项仍不是开发授权。本次不改《产品需求确认》或界面规格，避免把评审意见冒充用户已确认规则。

## 5. 原功能报告全部问题的去向

| 原编号 | 当前状态与处理 | 归并 |
| --- | --- | --- |
| UX-01 | 链路仍在；全库直排为规则变更候选 | O-02 / C-11 |
| UX-02 | 当前餐次管理仍无快照做法入口 | O-02 |
| UX-03 | 勾选已实现；入库尚未提供，单独列候选 | O-05 / C-09 |
| UX-04 | 快捷消耗尚未提供，能力候选 | O-04 |
| UX-05 | 预选及跳点单已被独立面板替代；入口仍在库存列表后 | O-04 |
| UX-06 | 逐月翻页仍在 | O-02 |
| UX-07 | 横向设置分页仍在 | O-07 |
| UX-08 | 餐次选择控件及顶部默认早餐仍在 | O-02 |
| F-01 | 独立状态仍需解释；不要求复现旧设备的具体 0/14 数量 | O-01 |
| F-02 | 确认菜单及同步相关命名仍有歧义 | O-01 |
| F-03 | 已买已实现；实际入库及手动采购项为扩展，不记作漏实现 | O-05 |
| F-04 | 长期维护成本仍在 | O-04 |
| F-05 | 旧全选严格筛选、过期候选问题已被新推荐替代；仍不代表数量齐全 | O-04 |
| F-06 | 修改方式与重算仍耦合；细化行为属候选 | O-04 / C-14 |
| F-07 | 直接到期日输入仍为候选 | O-04 |
| F-08 | 补缺汇总与输入复用仍为候选，明细保留在高级计算 | O-03 |
| F-09 | 后台提醒保留待验，不因视觉整理结案 | O-07 |
| FE-01 | 覆盖草稿路径仍在，优先修复候选 | O-06 |
| FE-02 | g/kg 不换算仍在，但符合当前规则，调整问题性质和优先级 | C-10 |
| FE-03 | 先成功后持久化的路径仍在 | O-08 |
| FE-04 | 图片加载故障传播仍为待验证风险 | O-08 |
| FE-05 | 全量保存与图片内存仍为待验证风险，未证明卡顿 | O-08 |

美学检查第 2 节共性进入 O-08，第 3 节进入 O-01/O-06，第 4 节进入 O-02，第 5 节进入 O-04，第 6 节进入 O-05，第 7 节进入 O-03，第 8 节进入 O-06/O-07；第 9 节的排序由下节统一。两份报告中单独出现的问题也已保留，没有仅保留交集。

## 6. 统一优先级与交付边界

| 层级 | 内容 | 处理方式 |
| --- | --- | --- |
| A：正确性优先 | FE-01 草稿保护、FE-03 保存反馈；FE-04 图片故障验证后修复 | 与视觉变更分开提交和测试，不因美学工作延后数据保护 |
| B：主要 UI 设计 | O-02 周菜单、O-03 回顾，其次 O-01 点单、O-07 设置 | 一次完成页面链路和视觉方案；改变已确认结构前明确更新设计基准 |
| C：局部精修 | O-04 冰箱、O-05 菜篮子、O-06 库与导入、O-08 视觉规则 | 沿用新紧凑成果，统一控件后检查相邻页面，不再次全局压缩 |
| D：能力扩展 | 直排全库、主动入库、快捷消耗、到期日、g/kg、营养复用、手动采购项 | 单独确认业务口径，不作为美学任务的默认附带工作 |
| 独立可靠性验收 | 后台提醒、真实 AI/网盘链路、图片和菜谱规模 | 可与设计工作分别推进；不将历史测试或模拟服务写成新的通过结论 |

后续验收至少覆盖 320/360/390/430 宽度、当前手写字体及普通系统字体、默认及放大字号、长内容、空/部分/满数据、软键盘、系统返回、离线与错误。比较密度必须使用相同设备条件和样本；“5 道点单、8 道菜谱、9 项采购”是既有常规样本参考，不是无限长名称也必须满足的硬指标。

本次只做文档核对，因此不运行构建、Android 同步或业务测试，不新增仅验证文字的测试。

## 7. 来源与证据

### 7.1 文档与源码

- [原功能评估](用户体验与功能评估-2026-09-22.md)：22 项原始编号、2026-09-22 真机与独立用例记录。
- [原美学检查](美学检查.md)：2026-09-23 01:43:48 记录，主观审美与截图分析，不是交互验收。
- [产品需求确认](产品需求确认.md)：按日期明确更新的增量约束；旧段落不单独作为最新行为依据。
- [V1.2.1 界面设计](V1.2.1-界面设计.md)：色彩、触摸、字体、五餐和营养基准。
- [开发进度](开发进度.md)：采购勾选、推荐、紧凑布局、统一导入及菜谱入口的历史验收记录。
- [App.jsx](../src/App.jsx)、[domain.js](../src/domain.js)、[storage.js](../src/storage.js)：状态、采购/推荐、草稿及存储核对。
- [MobileWeek.jsx](../src/components/MobileWeek.jsx)、[NutritionPanel.jsx](../src/components/NutritionPanel.jsx)、[StockFields.jsx](../src/components/StockFields.jsx)、[SettingsPanel.jsx](../src/components/SettingsPanel.jsx)、[Pickers.jsx](../src/components/Pickers.jsx)、[mobile.css](../src/styles/mobile.css)：页面与选择器行为、紧凑样式核对。

### 7.2 本次实际查看的已有截图

| 截图 | 核对目的 |
| --- | --- |
| [点单](../.android-tools/device-logs/compact-stock-20260923/order-density.png) | 新密度、黄色加号、零份确认栏 |
| [冰箱](../.android-tools/device-logs/compact-stock-20260923/fridge-three-actions.png) | 新顶部、三入口和风险状态 |
| [菜篮子](../.android-tools/device-logs/compact-stock-20260923/basket-checkbox.png) | 已有勾选、行距和重复待买文字 |
| [营养回顾](../.android-tools/device-logs/compact-stock-20260923/nutrition-review.png) | AI 区与每日营养的顺序、弹窗空间 |
| [更新后的菜谱库](../.android-tools/device-logs/library-final-20260923/recipe-library.png) | 补齐原美学报告的实屏缺口，核对 8 道样本与统一导入入口 |

截图在本地忽略目录，不随本文提交；路径存在不代表采集进程仍运行。截图中系统字体及字号属于设备条件。本次没有重新操作手机，不把以上历史截图表述为本轮真机验收，也不重新宣称原报告的测试结果覆盖当前提交。
