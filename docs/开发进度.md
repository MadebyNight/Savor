# 开发进度

当前真机状态见文末“真机补验”；此前无设备记录是历史状态。当前开发状态见本节；其后为按日期保留的历史过程，旧阻塞和会话编号不再表示仍在运行。

## 2026-09-21 确认菜单紧凑条目

- 用户确认 `docs/preview/selection-items.html` 的 A 方案：菜品按内容宽度横排、自动换行，单份省略数量，多份标记数量；点击后显示加减，减至 0 移除并补位。已替换正式确认菜单的逐行列表，保留采购同步及清空确认规则，键盘焦点在移除后转移到剩余菜品或确认按钮。
- 浏览器专项 `e2e_selection.py` 通过：320/360/390/430、横屏、1.15 文字放大模拟、完整长菜名、多位数份数、持久化、确认同步及空清单取消。既有 `e2e_confirm.py`、`e2e_back.py` 回归通过；前端构建、Capacitor sync、Android assembleDebug/testDebugUnitTest 通过。
- OPPO PGAM10 当前系统字体及 1.15 字号下实测：窄屏多份数量改到菜名下方后，首行可显示 3 道示例菜，长菜名不截断，份数加减与移除通过，无横向溢出。原始业务数据在同签名升级及测试恢复后哈希一致；临时示例未保留。
- 安装包 `食光-V1.2.1-菜单条目.apk` 已覆盖安装，沿用原 legacy debug 签名；SHA256 为 `2a1c6bb69634642badc8028dbe70d801763dc016db9cbecf700d47db885ca6e6`。测试日志 `.android-tools/selection-*.log`，持续手机日志 `.android-tools/device-logs/20260921-232304/`；未见 AndroidRuntime 崩溃，保留 WebView variations seed 文件提示记录。临时截图、私有数据备份和真机脚本逐个清理，预览页面作为设计依据保留。

## 2026-09-21 最终预览一致性修复

- 用户反馈冰箱分类占满屏幕后，确认 V1.2.1 横排覆盖未清除旧纵栏方向。已统一手机样式，补齐冰箱搜索与摘要、连续库存列表、状态标签与风险提示；周菜单恢复日期范围/日期条/标语与道数/五餐标签结构，保留历史和营养回顾；识别库存采用同行勾选、分类/数量/期限摘要与按需编辑，共用搜索框清除旧阴影和焦点色冲突。
- `e2e_layout.py`、`e2e_five_meals.py`、`e2e_mobile.py`、`e2e_food_storage.py`、`e2e_ai_review.py`、`e2e_back.py` 六套生产预览回归通过。新增检查覆盖 320/360/390/430 宽度、844×390 横屏、放大字号、冰箱首屏位置、组合筛选空状态、排序后原批次编辑、五餐十道菜首屏、餐次选择、翻周、核对行及菜篮子样式隔离。16 项相关 Node 单测通过。
- 已按 build → cap sync → Android assembleDebug/testDebugUnitTest 验证；Android 测试任务复用有效的既有 5 项通过缓存。日志与新验收截图位于 `.android-tools/layout-fix/`，周菜单截图位于 `.android-tools/v1.2.1/five-meals-{360,390}.png`。初次新测试中的动效取坐标与嵌套 summary 定位问题已修正，最终回归通过；没有将测试成功代替截图检查。
- 修复包：根目录 `食光-V1.2.1-界面修复.apk`，17,672,453 bytes，SHA256 `117a517f5241ed818c2c924acfb1c4c2bce64a05b6dad0b40ff2f9872d6f389f`。使用已有 legacy debug 证书，与原 V1.2.1 签名一致，v2/v3 验证通过；108 个 APK 前端资源与 dist 逐字节一致。版本仍为 1.2.1 / 4，已同签名覆盖安装至 OPPO PGAM10，未发布。
- 未改数据库、采购和营养计算规则；浏览器测试使用独立上下文；真机测试先备份，临时数据测试完成后核对原业务状态、偏好和加密凭据恢复一致。构建临时 S: 映射已撤销，SDK 本地路径已还原；保留最终验收日志和交付包，未删除用户原有未跟踪文件。

## 2026-09-21 布局修复真机补验与清理

- OPPO PGAM10，实际 CSS 视口 320×680，系统手写字体、字号比例 1.15。冰箱横排分类、首屏库存、搜索与空结果、原批次编辑入口、添加面板真实软键盘与系统返回、五个主页面无水平溢出通过；临时五餐十道菜首屏可见，餐次选择、一周总览返回、历史入口通过。
- 真机截图发现识别核对分类逐字换行，已补 ≤374px 的名称独立一行、分类/数量/期限下一行布局，并增加浏览器断言。最新 APK 再次覆盖安装，预置食材草稿的同行勾选、字段编辑、部分保存和剩余草稿保留通过；此项未调用 AI，不计作真实识别服务验收。
- 原业务数据、原识别草稿、完整偏好及加密凭据恢复一致。系统禁止 shell WRITE_SETTINGS，未强行变更设置；仅验证当前 1.15 倍字号，其他系统字号档位仍待手动测试。测试中手机息屏导致自动化等待超时，唤醒后重测通过。日志未见应用崩溃，WebView 有 variations seed 文件提示。
- 按用户要求，逐个删除本轮 17 张浏览器/真机截图、私有备份和临时测试脚本、10 份被替代的中间日志；没有使用通配符、删除循环或递归删除。上节截图路径仅说明当时检查来源，文件不再保留。最终结果在 `.android-tools/layout-fix/device-result.json`、`device-review-final.log` 等日志中；持续手机日志位于 `.android-tools/device-logs/20260921-223158/`。

## 2026-09-21 V1.2.1 开发补全

- P1–P5 功能代码已接入：独立识别、紧凑表单与五餐、FDA 参考保存期、CoFID 离线营养、主动 AI 补缺、可离线查看的周报及双渠道提醒。完整实现、字段及未验收清单见 `V1.2.1-TODO.md`。
- 50 项 Node 测试、18 套浏览器脚本（含 9 个边界用例）、5 套生产预览回归通过；Android assembleDebug/testDebugUnitTest 通过，包含 4 项新的周界/跨年/DST 日历测试和 1 项既有模板测试。日志与截图保留在 `.android-tools/v1.2.1/`。
- npm / Android versionName 为 1.2.1，versionCode 4，应用 ID 保留。使用原 legacy debug 签名生成根目录 `食光-V1.2.1.apk`（16,565,773 bytes），证书与 `食光-V1.1.2.apk` 相同；这是受控开发者调试包，未发布公开版。
- APK SHA256：`80c560eeae4063ebf7426e51e581fd2bc9f403ba01efa3ceb7b824e61e9e4190`。签名 v2/v3 通过；99 个包内前端文件与 dist 逐字节一致，签名前后 manifest 相同，versionCode/权限已核对。Vite 单块约 500kB 的体积提示不影响构建；本轮未为消除提示增加拆包复杂度。
- 每批独立提交，未推送。原有 `tests/android-native.mjs` 和接入图片保持未跟踪。Windows 构建用临时 S: 映射及既有 ASCII 缓存，结束后恢复 SDK 路径并解除映射。
- 当前 adb 无设备，未安装；通知权限/前后台/进程回收/重启、系统字体、真实拍照、覆盖升级及真实 AI/坚果云仍待验证。测试未请求真实服务。
- 历史失败缓存和测试/构建中间目录的删除被自动审批拒绝（blocked by policy），未绕过，清理未全部完成；APK、官方参考来源及必要验证证据保留。

## 2026-09-20 设置分页、相册入口与 DeepSeek 小票验证

- 按本轮确认方案将设置拆为“识别、AI 配置、备份恢复、坚果云同步”，每次进入默认识别；切页保留表单和草稿，移动端去掉重复标题。图片入口分为相册选择和拍摄，支持预览、同图重选、取消保留及读取失败提示；读取时禁止发送旧图。
- 官方资料核验：`deepseek-flash` 当前支持原生图片输入，使用 `image_url` 内容块的 Chat Completions 协议正确。参考 [视觉文档](https://api-docs.deepseek.com/zh-cn/guides/vision/) 和 [V4.1 Flash 发布说明](https://api-docs.deepseek.com/news/news260910/)。未因旧渠道测试失败改用其他模型。
- 使用 OPPO PGAM10（`ea767f86`）最新小票，原生请求约 12 秒返回 HTTP 200、9 项食材，并通过实际字段校验；本轮未复现用户最初的请求失败，不能确定原故障原因。保留已有可用请求协议，补齐 HTTP 参数/权限/余额/大小/限流/服务端错误提示；模型输出截断不得进入完整草稿。
- 新版真机通过系统相册选择最新图片（289×657）、预览、确认发送、HTTP 200、9 项可编辑食材草稿；未自动入库。系统相机启动与取消通过，取消后原图及 9 项草稿保留。实际快门拍摄没有执行，不能算完整拍摄取图验收。
- 32 项 Node 单元测试通过；设置分页专项，以及 AI 连接、识别、核对窗口、业务确认、核心流程、返回键、手动同步、启动同步共 9 套浏览器回归通过。启动同步测试增加开发页面加载等待，故意挂起响应的场景不等待网络空闲。
- 前端 build、Capacitor sync、Android assembleDebug/testDebugUnitTest 通过；使用既有 S: 工作区映射方案及 `.android-tools/gradle-home-ascii` 缓存。Android JVM 测试仍是既有模板测试，业务验证以 Node、浏览器和真机证据为准。沿用 legacy debug 签名覆盖安装，无卸载或清空数据；升级及识别前后业务数据 SHA-256 一致。
- 本轮日志与脱敏结果保存在 `.android-tools/device-logs/20260920-102726/`，仅记录状态、阶段、条目数及数据摘要，不记录 Key、请求头或小票正文。保留采集供后续手动验证。
- 最终交付 `食光-debug.apk` 已覆盖安装；包内前端资源与最终 `dist/` 逐字节一致。强制结束应用后重启，默认进入识别页时图片及 9 项草稿仍在，业务数据摘要不变；手机停留在食材核对窗口，供用户继续核对。

## 2026-09-19 Android 系统返回修复

- 原因：页面/弹窗仅更新 React 状态，没有消费 Android 返回事件。新增 `useBackHandler`，共用 Dialog 按嵌套深度优先关闭最上层，侧栏复用同一入口；设置、菜谱编辑和周菜单总览分别返回上级，草稿保留，确认框按取消处理。
- `MainActivity` 使用 AndroidX `OnBackPressedDispatcher`，键盘可见时先收起，否则派发可取消的 `shiguang:back`；前端未消费则执行系统默认返回。五个底部导航页作为根页面，不新增依赖、不修改业务数据格式。
- 验证通过：`tests/e2e_back.py`（生产预览，覆盖根页放行、设置、草稿、嵌套取消、库存、周总览/选菜、连续返回、重复开关、页面卸载及按钮返回混用）；原有 `tests/e2e_confirm.py` 通过。测试使用独立浏览器上下文，没有修改设备业务数据。
- 按顺序完成 `npm run build` → `npx cap sync android` → 离线 Gradle `assembleDebug testDebugUnitTest`；JVM 单测仍为既有模板测试，不作为返回功能验收。APK 在 `android/app/build/outputs/apk/debug/app-debug.apk`，内含 35 个前端文件与当前 dist 逐字节一致；`git diff --check` 通过。构建临时 S: 工作区映射已解除。
- 当前 adb 无设备，尚未安装本轮 APK；原生返回键、返回手势、真实软键盘与根页面退后台行为待真机验收。浏览器协议测试和 APK 构建通过不等同于真机问题已验证解决。

2026-09-16，MVP 开发中，未宣称完成。

## 已有证据

- bb8213c：需求收口与前端第一批功能提交。
- 前端生产构建通过；5项领域单元测试通过；Capacitor sync通过。
- 菜谱编辑删除、采购快照、真实周菜单、份数、历史复制、库存日期和缺口已实现，仍需E2E。
- 原生SQLite、Keystore、私有图片、HTTP桥接及仪器测试已写入，尚未原生构建完成。
- AI服务、设置、备份基础入口已写入，正在集成友好草稿编辑、原生导出和同步。

## 进行中

- Android代理下载Gradle/JDK和准备模拟器；Gradle session29189仍需轮询，不能重启未知下载。
- 主代理集成存储、AI、备份、WebDAV；前端代理DraftEditor；原生代理完成边界测试。
- 小红书按最新指示优先开源复用，已核验xpzouying/xiaohongshu-mcp和DeliciousBuding/xiaohongshu-skill，依赖外部浏览器/登录环境，尚未找到适合直接APK运行方案。不得将暂缓解释为用户永久取消需求。

## 待用户介入 / 外部验收

- 真实DeepSeek账户Key及额度：可用模拟响应测试UI与协议，但不能冒充实际服务验收。
- 真实坚果云WebDAV应用密码及双设备：本地协议测试后仍需实际账户验证。
- 当前adb无设备；优先自行准备模拟器，若不可用再记录真机安装/升级E2E待办。

## 剩余验收

所有产品需求逐项覆盖、Android构建、安装重启升级离线E2E、备份恢复、同步冲突、真实服务可用性。未验证项保持未完成。

## 2026-09-16 后续进度

- d1e4123 / 3bf713f：原生SQLite、凭据、图片、导出分享及AI/备份/WebDAV基础已提交。
- 领域+同步测试共14项通过，含并发412、图片损坏、10版本清理、旧菜单迁移。
- 浏览器核心E2E已通过，AI mock E2E通过；手机设置入口隐藏问题已修复，待复验。
- 公开网页提取复用@mozilla/readability，避免自行实现通用正文算法；小红书登录/评论抓取不在当前可验证能力内。
- Android Java21/Gradle已准备，构建64891运行；模拟器66257和system image95559正在下载，需继续轮询。未得到APK或Android E2E成功证据。

## Android 当前阻塞证据（2026-09-16）

- 前端生产构建、领域/同步单元测试（14项）、核心/AI/链接/WebDAV浏览器E2E均已通过。
- `scripts/android-build.ps1` 已统一工作区JDK21、SDK和Gradle流程。
- Android assembleDebug 尚未通过。Gradle 8.14.3 在 `.android-tools/gradle-home-clean/caches/8.14.3/transforms/` 报 `AccessDeniedException`，临时目录无法移动到 immutable 目录；换缓存、单线程、禁用watch后仍复现。
- Android 35 x86_64模拟器镜像和emulator仍在工作区断点下载；当前无adb设备，故connectedDebugAndroidTest尚无证据。
- 下一步自动尝试Gradle 8.13兼容路径；若仍失败，需要用户在本机关闭占用该目录的安全软件/索引或提供真机USB调试。此项是环境介入，不代表代码构建失败。

- 新证据：Gradle8.13完整下载，本地直启/ASCII subst路径均已进入构建，但checkDebugAarMetadata仍因transforms原子移动失败。中文路径不是已证实根因；FullClasspathForDexingTransform配置无效已撤回。模拟器已解压且WHPX可用，system-image继续下载。
- APK已成功生成：`android/app/build/outputs/apk/debug/app-debug.apk`，约6.7MB；使用JDK21、Gradle8.13，经ASCII盘符和缓存转换目录修复后构建完成。
- `assembleDebug`与`testDebugUnitTest`成功；Android仪器测试尚未执行。
- 模拟器可执行且WHPX可用，但手工解压的system image未被avdmanager识别，当前AVD启动提示kernel路径缺失；需要补齐SDK package元数据或使用真机。
- 2026-09-16复核：APK已成功构建（6,744,246 bytes），`assembleDebug`与`testDebugUnitTest`通过。
- 直接启动模拟器可识别kernel和WHPX，但G盘工作区剩余约3.8GB，Android 35默认userdata需约7.3GB；`-partition-size 2047`仍被当前AVD配置要求拒绝。当前工作区不允许使用其他盘，故Android connected E2E受磁盘容量阻塞。需用户释放当前工作区所在磁盘空间或连接真机USB调试后继续。

2026-09-17 复核：APK 静态验证通过（app-debug.apk，APK Signature Scheme v2）。`connectedDebugAndroidTest` 未执行成功：无可启动 AVD/adb设备，且离线Gradle缓存缺少AGP依赖。G盘剩余约3.8GB，Android system image 的userdata空间不足。代码、浏览器E2E和静态APK证据完整；待释放工作区磁盘或连接真机后执行最后的Android E2E。

## 真机验收（OPPO PGAM10）

- 最新前端build→Capacitor sync→app/test APK构建成功（此前已安装APK资源较旧，已修正）。
- 发现原/新debug签名不一致，使用与已安装包匹配的旧debug密钥重新签名后保留数据覆盖安装；密钥仅保存在忽略目录。
- adb am instrument 真机通过4项测试（包名、非法JSON不覆盖、偏好缺失/图片路径与格式拒绝、导出路径拒绝）。
- tests/android-device.mjs 真机WebView/CDP操作通过：UI新建菜谱→SQLite保存；UI确认采购→快照；force-stop后重启持久化。测试前状态备份在忽略目录，测试后恢复并对比完全一致。
- 未覆盖：图片选择/拍照、系统导出分享、完整离线周菜单库存流程、真实AI与WebDAV。不能把4项异常输入测试当作完整Android E2E。

## 2026-09-18 手机端正式布局接入

- 按用户确认的原型接入正式 React 入口：紧凑顶栏、底部五入口、常驻左分类、小图菜品列表、库存折叠编辑、采购分类列表、菜谱列表进入编辑。
- 周菜单增加按日三餐与纵向一周总览，从餐次弹层选择已确认菜品；复用原有真实周、快照、餐次份数与历史复制逻辑。未改变业务数据格式、SQLite 或密钥存储。
- `node --test src/domain.test.js src/sync.test.js`：15 项通过。`tests/e2e.py` 与 `tests/e2e_ai.py` 通过，覆盖核心增改删、采购抵扣、历史复制、备份恢复、AI 模拟响应与草稿。
- 新增 `tests/e2e_mobile.py` 通过：首屏分类与列表、搜索食材、切页保留筛选、库存抵扣、按日/总览、草稿恢复、编辑、删除后菜单快照、清空采购不清菜单、重载保留。360×800、390×844、430×932、844×390 五页及设置均无水平溢出；点单文字放大 30% 无水平溢出。
- 人工查看正式页面截图，产物在 `.android-tools/mobile-review/`。独立原型仍保留在 `prototypes/`，不作为正式业务入口。
- 完成 `npm run build` → `npx cap sync android` → Gradle `assembleDebug`。`testDebugUnitTest` 通过，但当前 Android JVM 测试仅有一条模板加法测试，不代表业务或设备验收。
- Windows PowerShell 5 直接运行现有 `scripts/android-build.ps1` 出现 UTF-8 中文解析错误；直接运行本地 Gradle 时，中文路径还导致测试 worker 类加载失败。使用临时 `S:` 映射工作区、本地 JDK21、Gradle8.13、`.android-tools/gradle-home-ascii` 缓存，并加 `--offline --no-daemon --max-workers=2` 后成功，未更改全局工具安装。
- 交付 APK：`.android-tools/mobile-review/shiguang-mobile.apk`，使用已有 legacy debug 密钥签名，证书摘要与之前保留的 `installed-base.apk` 匹配；签名验证通过，包内 29 个前端资源逐字节匹配本轮 `dist/`。密钥未写入代码或文档。
- 本轮 `adb devices` 无设备，因此未安装升级、未执行设备仪器测试；真实软键盘、Android 安全区、系统返回和图片/分享流程待真机验证。此前真机证据不计作本轮界面验收。

## 2026-09-19 V1.1 真机升级与回归

- 版本更新为 npm `1.1.0`、Android `versionName 1.1` / `versionCode 2`，用户授权提交与覆盖安装。
- 从连接的 OPPO PGAM10 提取实际已安装 APK，核验证书后使用匹配的 legacy debug 密钥签名，经 `adb install -r` 成功覆盖升级。未卸载、未清空应用数据，仍为个人安装调试包，不是应用商店正式签名发布。
- 升级前保留应用私有文件备份、旧 APK 及业务 JSON 基线；升级后 SQLite 业务状态与基线一致。恢复材料和证据在忽略目录 `.android-tools/v1.1-acceptance/`，不提交私有数据及密钥。
- 真机首次回归发现冰箱弹窗偏移：生产构建将 `translate: none` 合并为 transform，但居中工具类的独立 translate 仍生效。在手机弹窗规则中清零两个位移变量修复，未改变业务逻辑或数据库结构。
- 补充生产预览弹窗边界回归（含 336px 压缩高度），手机端 E2E 通过；领域/同步单测 15 项通过。WebDAV 模拟 E2E 已更新菜谱入口并通过，包含首次同步、冲突及损坏数据保护。
- 重新完成 build → Capacitor sync → Android assembleDebug / testDebugUnitTest，重新签名覆盖安装。Android JVM 单测仍仅为模板测试，不作为业务验收证据。
- `tests/android_upgrade.py verify` 在真机 WebView 通过：新建菜谱写入 SQLite；按食材搜索、确认两份菜；键盘弹出时弹窗边界正常且可滚动确认；库存 30g 抵扣 200g 需求后缺口 170g；按日排餐、餐次三份、一周总览；force-stop 后持久化；编辑/删除不破坏历史餐次快照；设置入口可达。
- 验收完成后恢复全部原始业务状态，并核对规范化 JSON 摘要一致（报告 `restored: true`）。评审截图与 V1.1 APK 保留为交付证据；此前确认删除的 `.android-tools/e2e/backup.json` 未重建。
- 本轮未完成：真实 AI 账户请求、真实坚果云及双设备同步、图片选择/拍照、系统导出分享、全面系统返回/安全区与断网矩阵验证；不宣称全部功能均已验收。未改动用户原有 `tests/android-native.mjs`。

## 2026-09-19 业务弹窗统一

- 用户确认排查并统一业务弹窗。11 处 `window.confirm` 已替换：删除菜谱、清空采购、覆盖目标周、AI 发送、识别草稿覆盖、识别类型切换、输入正文替换、导入备份、恢复导入前数据、恢复云端版本、恢复待发布副本。扫描 `src/` 与原生应用源码，无残余系统业务确认调用。
- 新增共用 `useConfirm`，复用现有 Dialog；默认聚焦取消，关闭/Escape/卸载视为取消，重复请求不执行操作。危险操作使用红色明确动作按钮。嵌套确认启用独立遮罩；设置和同步面板各持有确认状态，不引入全局弹窗服务或新依赖。
- 现有菜谱详情、库存录入、排餐、历史和导出预览继续使用应用内弹窗；普通反馈继续使用应用内 toast；系统文件选择、拍照、文件保存、分享及权限授权保持系统实现。
- `tests/e2e_confirm.py` 生产预览通过，覆盖取消不改数据/不发请求、周覆盖、AI 授权与草稿、识别类型、正文替换、备份恢复、模拟云端/副本恢复；更新手机回归覆盖删除的取消/关闭/Escape/确认和采购清空的取消/确认。手机回归（含四种视口和字体放大）、核心 E2E、AI mock、WebDAV mock（27 请求）、15 项领域/同步单测通过。
- build → cap sync → assembleDebug 成功。真机 OPPO PGAM10 原签名覆盖安装 `.android-tools/dialog-acceptance/shiguang-dialogs.apk`；测试删除时显示应用内确认并可取消，再确认删除后历史快照仍在；全程未触发原生业务对话框事件。人工检查真机确认截图，遮罩层级、取消和删除按钮正常。
- 真机验收原始数据已恢复且摘要一致（`restored: true`），本轮原始 APK/私有备份、报告和截图保留在 `.android-tools/dialog-acceptance/`；未覆盖 V1.1 的交付产物、Git 标签及用户原有 `tests/android-native.mjs`。当前为 V1.1 基础上的待提交修复，未提升版本号。
- 真实 AI、坚果云账户及系统图片/分享能力未因本次弹窗改造获得新的验收证据，仍保留之前的待验收状态。

## 2026-09-19 Android WebDAV ProtocolException 修复

- 用户提供“操作失败：ProtocolException”。新增 Android 仪器测试在 OPPO PGAM10 直接证实：旧 `HttpURLConnection.setRequestMethod("MKCOL")` 在连接前抛出 ProtocolException，根因是原生请求方法限制，不是该异常本身证明账号密码有误。
- 原生请求改为固定版本 OkHttp 4.12.0，保持超时、禁用重定向、UTF-8 请求体、状态码及小写响应头接口；关闭连接失败自动重试，避免写请求被透明重发。未修改凭据存储、业务数据或同步决策规则。
- 新增 `ShiguangNetwork` 脱敏日志，仅允许请求方法、prepare/execute/read/response 阶段、HTTP 状态码和异常类名；不记录 URL、账号、密码、Authorization、请求/响应正文和原始异常消息。调用方错误包含方法/阶段/异常类型。
- 仪器测试使用设备内 MockWebServer；仅 debug 配置允许 localhost/127.0.0.1 明文 HTTP，其他域名仍禁用明文，release 不含此配置。测试覆盖 MKCOL/PUT/GET/DELETE/POST/PROPFIND、中文正文、认证及条件头、ETag、401/403/405/412/500、禁止自动重定向、错误脱敏及原有数据保护；真机共 7 项通过（含原模板包名测试）。
- `npm run build` → `npx cap sync android` → `:app:assembleDebug :app:assembleDebugAndroidTest :app:testDebugUnitTest` 通过。首次使用未限定模块的 assembleDebugAndroidTest 连带触发附属 Cordova 测试模块的 Kotlin 重复类错误；应用模块本身通过，未为此修改附属模块或全局依赖。
- APK `.android-tools/webdav-acceptance/shiguang-webdav.apk` 与实际旧包核验证书一致后覆盖安装，无卸载/清空。保留本轮安装前的私有数据备份和旧 APK，不覆盖旧 V1.1 交付文件和标签。
- `tests/android_sync_check.py` 使用手机已保存配置仅点击“检查并同步”，真实服务器返回 `method=MKCOL stage=response status=401`。原 ProtocolException 已消除，但真实账户认证尚未通过；检查确认官方 endpoint 正确、账号为非空邮箱格式且无首尾空格，不读取/输出应用密码。没有选择上传/恢复，业务数据摘要与安装前一致。
- 待用户在应用中重新核对该账号对应的有效坚果云应用密码后继续真实同步验收；不能将 401 或原生 mock 测试记为坚果云同步已可用。此次修复及此前弹窗修改尚未提交。


## 2026-09-20 同步确认弹窗与启动同步

- 手动检查后的版本选择改为居中独立弹窗，展示本地/云端概况及方向说明；有云端版本时始终提供“上传到云端”和“下载到本机”，无版本时说明原因。取消/系统返回不执行同步，同步中禁止关闭及重复操作。
- SyncPanel 作为应用级持久组件，在本地数据加载完成后、已有配置与凭据时，每次冷启动检查一次；设置页通过 portal 显示配置，进出设置不重复启动检查。启动自动同步默认开启，可关闭，不做后台常驻轮询。
- 基线记录绑定服务器与账号。可信基线下单端变化自动上传/下载；首次连接、账号切换、旧版无账号绑定基线或双方变化需确认。恢复历史/待发布副本后，下次启动不自动用云端最新版本覆盖。
- 检查/下载/保存恢复期间保护新增本地修改，恢复前保留备份；本机选菜和编辑草稿保留。网络失败提示并允许继续离线使用。
- 新增 tests/e2e_sync_startup.py（Vite 开发服务器，新启动实例运行）：覆盖冷启动上传/下载、首次连接、账号切换、冲突、关闭开关、503、不重复检查、下载期间新增库存、取消/系统返回、忙碌状态与三个视口居中。仅注入模拟凭据，不连接真实坚果云。
- 15 项领域/同步单测、既有双设备 WebDAV 回归（27 请求）、应用确认回归通过。前端 build、Capacitor sync 和 Android assembleDebug 通过。手机弹窗截图保留在 .android-tools/sync-dialog-review.png。
- 本次未安装到真机，也未使用真实账号验证启动同步；模拟测试和构建不代表真实网盘验收。
## 2026-09-20 验收阻断修复

- 验收报告及失败回归基线提交：`0a50c95`。后续修复公开链接 HTTPS 跳转与移动页面请求、AI 字段校验和异常草稿保护、备份嵌套字段及重复菜谱 ID 校验，保留无 ID 手动库存兼容。
- 新 APK 已安装 PGAM10；真实小红书分享链接 → 正文 → 当前文本 AI → 核对保存 → 重启保留通过。测试数据和偏好已恢复，日志继续采集。
- 29 项单元测试、原有 9 套浏览器回归、新增 9 个边界用例、Android 构建及真机核心/升级回归通过。系统栏改用 native 处理，新版冷启动未再出现安全区 CSS 注入异常。
- 真实视觉识别仍受供应商可用性阻断，不能宣称全量外部链路通过。详细范围、历史失败及证据见 `docs/E2E验收-2026-09-20.md`。

## 2026-09-20 V1.1.2 交互一致性

- 用户授权修复七项 UI 问题并指定 V1.1.2；已记录范围、验收标准、证据和限制，见 `docs/V1.1.2交互修复.md`。
- 完成共用按钮/焦点/复选框/toast/弹窗动效与关闭区域修复，版本提升为 1.1.2 / versionCode 3。
- 新增交互回归、六套既有浏览器回归、24 项相关单测及 Android 构建通过。手机同签名覆盖安装，实际界面和指针反馈通过，业务数据摘要保持一致。
- 已按用户明确选择加入“加密内置开发者配置”：设备内导出密文，密码解锁后写入独立 Keystore 槽，启停不覆盖个人配置。密码、真实 Key 不进入源码或 APK 明文；此方案用于受控分发，不能阻止 APK 持有人离线猜测密码。
- 36 项单测、新增开发者配置 UI 回归和真机解锁/重启/关闭恢复通过；全新浏览器无个人配置也可解锁实际密文。最终 APK 已重新构建并同签名覆盖安装，85 个前端资源逐字节匹配，业务与个人凭据保持不变。最终证据和摘要见版本文档，本轮按 V1.1.2 归档。

## 2026-09-20 GitHub 公开分发

- 按用户确认，仅向 `MadebyNight/Savor` 发布源码与无共享凭据公开 APK；不创建私有仓库，开发者版留本地自行分发。
- 增加独立公开构建、入口开关及历史产物检测，从隔离工作区完成 release 构建。此前已进入 APK 的业务确认、OkHttp 修复与配套回归源码补齐归档。
- 36 项单测、公开与既有界面回归、release 构建及打包内容扫描通过；代码历史未发现真实 Key、解锁密码或密文内容。公开 release 本轮未安装到手机，避免替换现有开发者版。
- 发布附件仅为 `Savor-v1.1.2-public.apk` 与 SHA256 校验文件，版本与摘要见 `docs/V1.1.2交互修复.md`。

## 2026-09-21 V1.2.1 首批开发（历史阶段，已由顶部补全）

- 需求已完全确认，直接实施 P0–P3：独立识别入口与任务草稿迁移、配置返回、紧凑库存编辑/核对、五餐单日及总览。旧三餐 key、采购份数和历史快照保持；临期采用已确认分段阈值。
- 39 项 Node 测试、相关浏览器回归及生产样式回归通过；APK 构建通过，91 个前端文件与 dist 一致。原生构建沿用历史 S: 映射方案，映射已解除，没有连接设备、没有安装。完整范围与证据见 `V1.2.1-TODO.md`。
- 未完成：自动参考保存期的数据表与保存方式、离线营养算法、AI 补充/周报、双渠道提醒、全应用真机验收。不得将未估算占位当作营养功能完成，不提升发布版本。
- 历史缓存和构建中间目录清理被自动审批拒绝，删除未执行；明确保留为待处理事项。原有未跟踪测试、图片与已有 APK 均未删除。

## 2026-09-21 真机补验（20:30 起）

设备 OPPO PGAM10 / Android 14 / ea767f86。先启动日志采集，证据在 `.android-tools/v121-acceptance-2030/`，持续日志在 `.android-tools/device-logs/20260921-203028/`。

- 本次另存私有数据、已安装 APK 和业务基线，未覆盖既有备份；关闭自动同步后验证。原证书覆盖安装成功，业务摘要及加密凭据文件一致。既有上午升级记录同样显示业务/加密凭据保留；本轮是对当前安装状态的再次覆盖验证，未降级重造旧版环境。
- 冰箱拍照入口直接打开系统相机，实际快门、确认照片和应用预览回传通过；未请求 AI，不能称为真实图片识别服务验收。
- 通知权限拒绝后保留应用内提醒、系统通知未启用；允许后原生通知创建成功。返回前台显示目标周待回顾提示。真实点击通知进入 2026-09-21 周，查看后去重并取消对应通知，不依赖 AI。
- **后台定时投递未通过**：设置 20:39 的非精确 AlarmManager，系统 dumpsys 显示 OEM adjustment 将执行推迟约 3 天。通知展示和点击测试使用诊断调用触发，不能冒充定时、划掉任务或重启验收通过。证据 `alarm-policy.txt`。未增加精确闹钟权限或电池豁免；需针对这台 OPPO 的后台策略继续评估，不能宣称后台提醒已可靠。
- 通知点击第一次脚本检查失败是 attach 辅助函数再次启动 Activity 覆盖启动参数；改为直接连接通知启动的 WebView 后通过，未因此修改产品代码。进程是否实际回收未单独断言，因此不把该步骤标为完整冷启动验收。
- 结束恢复本轮业务基线与原 shared_prefs，逐文件比对通过；系统通知在系统设置恢复关闭（importance=NONE）。应用保持停止，避免恢复自动同步后主动联网。新增的唯一测试私有图片已单独删除，ADB 转发已移除，保留日志采集。
- 按用户要求逐对象删除：空探测目录 rename-probe-done 和设备测试照片成功；单目录 gradle-home-clean、单文件 Python 缓存仍被工具自动审批 blocked by policy 拒绝，未执行，未绕过。临时产物尚未全部清理。原有未跟踪验收脚本及接入图片未改动/提交。


## 2026-09-22 营养回顾与选择器真机验收

- OPPO PGAM10（ea767f86），当前系统字体、系统字号 1.15；真机 WebView/CDP 操作、触摸滑动和 adb Android 返回键验收。
- 初次覆盖安装发现新构建默认 debug 签名不兼容；使用工作区既有 legacy debug 密钥重新签名，覆盖安装成功，无卸载或清空。当前 `android/app/build/outputs/apk/debug/app-debug.apk` 已重签并安装，后续重新构建需注意保留同签名。
- 设置单行导航与滑动、目标周全部 7 天的切换、回顾分层、高级计算返回保留日期、食材表单边界、应用内分类和日期选择通过；已查看设备原生截图。
- 覆盖升级和验收前后业务 JSON 规范化哈希一致。测试期间临时关闭启动同步，结束已恢复原偏好；未写入测试菜谱、食材、菜单或报告。查看回顾会按正常业务规则标记该周已查看。
- 设备日志 `.android-tools/device-logs/20260922-004342/` 保持采集；截图和结果 `.android-tools/device-logs/review-acceptance-20260922/`；运行日志 `android-ui-review.log`。未见 AndroidRuntime 崩溃，记录 WebView variations seed 文件提示。
- 本次未发送真实 AI 请求，真实生成文案质量、云端服务可用性不在通过结论内。


## 2026-09-22 冰箱添加与采购进度优化

- 冰箱状态短文案、拍摄/相册/手动添加同排、独立匹配菜品面板；选图即发送的提示置于入口旁，取消系统选择留在冰箱。食材识别自动请求一次，已有草稿追加保留，异常/超限/取消不覆盖；菜谱识别授权保持不变。
- 采购勾选写入 SQLite 业务状态，随备份和同步保留，数量变化及条目移除清理勾选；只标记购买进度，不自动入库。历史/清空本周同排，移除重复保存说明。
- 21 项采购/保存期/同步单测、冰箱专项、保存期、五餐、识别、AI 核对、确认弹窗及布局回归通过。最终 build → cap sync → Android assembleDebug/testDebugUnitTest 通过，既有 JVM 测试仅作构建检查；APK 129 个前端资源逐字节匹配 dist，使用既有 legacy debug 签名覆盖安装。
- OPPO PGAM10 当前系统字体、1.15 字号：三项入口、状态短标签、采购勾选、推荐面板、历史/清空同排、日期及原生返回验收通过；勾选在 WebView 重载后保留。相机和相册实际打开后取消均停留冰箱、不启动识别。测试勾选恢复后业务数据一致，临时关闭的启动同步偏好已恢复。
- 自动上传、错误/取消、旧草稿追加用模拟服务验证；未向真实 AI 发送手机照片，云端识别能力不计入本轮通过结论。
- 日志与截图：`.android-tools/device-logs/fridge-acceptance-20260922/`、`fridge-refinement-*.log`；持续设备日志 `20260922-232443/`。未见应用崩溃，仅保留 WebView variations seed 文件提示。

## 2026-09-23 页面冗余文案清理

- 清理冰箱、识别、设置、提醒、菜谱编辑、选菜、分享、同步和营养回顾中的常驻技术说明与重复操作说明；简化 AI 操作确认，保留错误、空状态、数据缺失及覆盖确认。营养来源和覆盖项数仅在高级计算展示。
- 复用既有交互回归，未新增仅断言文案的测试。冰箱专项、设置、营养布局、AI 连接、营养流程、提醒共 6 组通过；build → cap sync → Android assembleDebug/testDebugUnitTest 通过。
- 使用原签名覆盖安装到 OPPO PGAM10，1.15 字号下检查设置横滑、目标周 7 日切换、高级计算返回、冰箱入口与表单边界；已查看设备截图，业务数据校验一致、同步偏好已恢复。未调用真实 AI，识别请求使用模拟服务验证。
- 结果与截图：`.android-tools/device-logs/copy-cleanup-20260923/`；命令日志：`copy-cleanup-*.log`；设备日志持续采集：`.android-tools/device-logs/20260923-000703/`。

## 2026-09-23 菜谱导入流程合并

- “导入菜谱”直接打开同页相册、拍摄和链接／正文单输入框；链接提取回填同框，移除图文／正文方式选择层。手机上去除重复标题，AI 配置靠右，沿用应用配色。
- 菜谱使用统一草稿槽，首次合并旧图文与正文的输入和识别结果，旧槽原件保留；图片与正文共用草稿，食材草稿仍独立。链接提取失败保留输入，取消后的迟到正文不会覆盖新输入。
- 6 项链接/草稿单测通过；设置、识别、AI 模拟、确认、冰箱、返回、边界、AI 核对、交互样式共 9 组 E2E 通过。最终布局调整后再次通过设置与响应式回归。
- build → cap sync → Android assembleDebug/testDebugUnitTest 通过，按原 debug 签名覆盖安装 OPPO PGAM10；字号 1.15 下检查导入布局，实际打开系统相机和相册后取消，原输入和图片保持。业务数据哈希一致，同步偏好已恢复，已查看最终设备截图。
- 真机未上传照片或调用真实 AI；链接与识别成功/失败使用模拟服务验证。日志 `import-*.log`，最终截图与结果 `.android-tools/device-logs/recipe-import-final-20260923/`，设备日志 `.android-tools/device-logs/20260923-003137/` 保持采集，未见 AndroidRuntime 崩溃。
