# 开发进度

## 2026-09-19 Android 系统返回修复

- 原因：页面/弹窗仅更新 React 状态，没有消费 Android 返回事件。新增 `useBackHandler`，共用 Dialog 按嵌套深度优先关闭最上层，侧栏复用同一入口；设置、菜谱编辑和周菜单总览分别返回上级，草稿保留，确认框按取消处理。
- `MainActivity` 使用 AndroidX `OnBackPressedDispatcher`，键盘可见时先收起，否则派发可取消的 `shiguang:back`；前端未消费则执行系统默认返回。五个底部导航页作为根页面，不新增依赖、不修改业务数据格式。
- 验证通过：`tests/e2e_back.py`（生产预览，覆盖根页放行、设置、草稿、嵌套取消、库存、周总览/选菜、连续返回、重复开关、页面卸载及按钮返回混用）；原有 `tests/e2e_confirm.py` 通过。测试使用独立浏览器上下文，没有修改设备业务数据。
- 按顺序完成 `npm run build` → `npx cap sync android` → 离线 Gradle `assembleDebug testDebugUnitTest`；JVM 单测仍为既有模板测试，不作为返回功能验收。APK 在 `android/app/build/outputs/apk/debug/app-debug.apk`，内含 35 个前端文件与当前 dist 逐字节一致；`git diff --check` 通过。构建临时 S: 工作区映射已解除。
- 当前 adb 无设备，尚未安装本轮 APK；原生返回键、返回手势、真实软键盘与根页面退后台行为待真机验收。浏览器协议测试和 APK 构建通过不等同于真机问题已验证解决。

2026-09-16，MVP 开发中，未宣称完成。

## 已有证据

- bb8213c：需求收口与前端第一批功能提交。
- 前端生产构建通过；5项领域单元测试通过；Capacitor sync通过。
- 菜谱编辑删除、采购快照、真实周菜单、份数、历史复制、库存日期和缺口已实现，仍需E2E。
- 原生SQLite、Keystore、私有图片、HTTP桥接及仪器测试已写入，尚未原生构建完成。
- AI服务、设置、备份基础入口已写入，正在集成友好草稿编辑、原生导出和同步。

## 进行中

- Android代理下载Gradle/JDK和准备模拟器；Gradle session29189仍需轮询，不能重启未知下载。
- 主代理集成存储、AI、备份、WebDAV；前端代理DraftEditor；原生代理完成边界测试。
- 小红书按最新指示优先开源复用，已核验xpzouying/xiaohongshu-mcp和DeliciousBuding/xiaohongshu-skill，依赖外部浏览器/登录环境，尚未找到适合直接APK运行方案。不得将暂缓解释为用户永久取消需求。

## 待用户介入 / 外部验收

- 真实DeepSeek账户Key及额度：可用模拟响应测试UI与协议，但不能冒充实际服务验收。
- 真实坚果云WebDAV应用密码及双设备：本地协议测试后仍需实际账户验证。
- 当前adb无设备；优先自行准备模拟器，若不可用再记录真机安装/升级E2E待办。

## 剩余验收

所有产品需求逐项覆盖、Android构建、安装重启升级离线E2E、备份恢复、同步冲突、真实服务可用性。未验证项保持未完成。

## 2026-09-16 后续进度

- d1e4123 / 3bf713f：原生SQLite、凭据、图片、导出分享及AI/备份/WebDAV基础已提交。
- 领域+同步测试共14项通过，含并发412、图片损坏、10版本清理、旧菜单迁移。
- 浏览器核心E2E已通过，AI mock E2E通过；手机设置入口隐藏问题已修复，待复验。
- 公开网页提取复用@mozilla/readability，避免自行实现通用正文算法；小红书登录/评论抓取不在当前可验证能力内。
- Android Java21/Gradle已准备，构建64891运行；模拟器66257和system image95559正在下载，需继续轮询。未得到APK或Android E2E成功证据。

## Android 当前阻塞证据（2026-09-16）

- 前端生产构建、领域/同步单元测试（14项）、核心/AI/链接/WebDAV浏览器E2E均已通过。
- `scripts/android-build.ps1` 已统一工作区JDK21、SDK和Gradle流程。
- Android assembleDebug 尚未通过。Gradle 8.14.3 在 `.android-tools/gradle-home-clean/caches/8.14.3/transforms/` 报 `AccessDeniedException`，临时目录无法移动到 immutable 目录；换缓存、单线程、禁用watch后仍复现。
- Android 35 x86_64模拟器镜像和emulator仍在工作区断点下载；当前无adb设备，故connectedDebugAndroidTest尚无证据。
- 下一步自动尝试Gradle 8.13兼容路径；若仍失败，需要用户在本机关闭占用该目录的安全软件/索引或提供真机USB调试。此项是环境介入，不代表代码构建失败。

- 新证据：Gradle8.13完整下载，本地直启/ASCII subst路径均已进入构建，但checkDebugAarMetadata仍因transforms原子移动失败。中文路径不是已证实根因；FullClasspathForDexingTransform配置无效已撤回。模拟器已解压且WHPX可用，system-image继续下载。
- APK已成功生成：`android/app/build/outputs/apk/debug/app-debug.apk`，约6.7MB；使用JDK21、Gradle8.13，经ASCII盘符和缓存转换目录修复后构建完成。
- `assembleDebug`与`testDebugUnitTest`成功；Android仪器测试尚未执行。
- 模拟器可执行且WHPX可用，但手工解压的system image未被avdmanager识别，当前AVD启动提示kernel路径缺失；需要补齐SDK package元数据或使用真机。
- 2026-09-16复核：APK已成功构建（6,744,246 bytes），`assembleDebug`与`testDebugUnitTest`通过。
- 直接启动模拟器可识别kernel和WHPX，但G盘工作区剩余约3.8GB，Android 35默认userdata需约7.3GB；`-partition-size 2047`仍被当前AVD配置要求拒绝。当前工作区不允许使用其他盘，故Android connected E2E受磁盘容量阻塞。需用户释放当前工作区所在磁盘空间或连接真机USB调试后继续。

2026-09-17 复核：APK 静态验证通过（app-debug.apk，APK Signature Scheme v2）。`connectedDebugAndroidTest` 未执行成功：无可启动 AVD/adb设备，且离线Gradle缓存缺少AGP依赖。G盘剩余约3.8GB，Android system image 的userdata空间不足。代码、浏览器E2E和静态APK证据完整；待释放工作区磁盘或连接真机后执行最后的Android E2E。

## 真机验收（OPPO PGAM10）

- 最新前端build→Capacitor sync→app/test APK构建成功（此前已安装APK资源较旧，已修正）。
- 发现原/新debug签名不一致，使用与已安装包匹配的旧debug密钥重新签名后保留数据覆盖安装；密钥仅保存在忽略目录。
- adb am instrument 真机通过4项测试（包名、非法JSON不覆盖、偏好缺失/图片路径与格式拒绝、导出路径拒绝）。
- tests/android-device.mjs 真机WebView/CDP操作通过：UI新建菜谱→SQLite保存；UI确认采购→快照；force-stop后重启持久化。测试前状态备份在忽略目录，测试后恢复并对比完全一致。
- 未覆盖：图片选择/拍照、系统导出分享、完整离线周菜单库存流程、真实AI与WebDAV。不能把4项异常输入测试当作完整Android E2E。

## 2026-09-18 手机端正式布局接入

- 按用户确认的原型接入正式 React 入口：紧凑顶栏、底部五入口、常驻左分类、小图菜品列表、库存折叠编辑、采购分类列表、菜谱列表进入编辑。
- 周菜单增加按日三餐与纵向一周总览，从餐次弹层选择已确认菜品；复用原有真实周、快照、餐次份数与历史复制逻辑。未改变业务数据格式、SQLite 或密钥存储。
- `node --test src/domain.test.js src/sync.test.js`：15 项通过。`tests/e2e.py` 与 `tests/e2e_ai.py` 通过，覆盖核心增改删、采购抵扣、历史复制、备份恢复、AI 模拟响应与草稿。
- 新增 `tests/e2e_mobile.py` 通过：首屏分类与列表、搜索食材、切页保留筛选、库存抵扣、按日/总览、草稿恢复、编辑、删除后菜单快照、清空采购不清菜单、重载保留。360×800、390×844、430×932、844×390 五页及设置均无水平溢出；点单文字放大 30% 无水平溢出。
- 人工查看正式页面截图，产物在 `.android-tools/mobile-review/`。独立原型仍保留在 `prototypes/`，不作为正式业务入口。
- 完成 `npm run build` → `npx cap sync android` → Gradle `assembleDebug`。`testDebugUnitTest` 通过，但当前 Android JVM 测试仅有一条模板加法测试，不代表业务或设备验收。
- Windows PowerShell 5 直接运行现有 `scripts/android-build.ps1` 出现 UTF-8 中文解析错误；直接运行本地 Gradle 时，中文路径还导致测试 worker 类加载失败。使用临时 `S:` 映射工作区、本地 JDK21、Gradle8.13、`.android-tools/gradle-home-ascii` 缓存，并加 `--offline --no-daemon --max-workers=2` 后成功，未更改全局工具安装。
- 交付 APK：`.android-tools/mobile-review/shiguang-mobile.apk`，使用已有 legacy debug 密钥签名，证书摘要与之前保留的 `installed-base.apk` 匹配；签名验证通过，包内 29 个前端资源逐字节匹配本轮 `dist/`。密钥未写入代码或文档。
- 本轮 `adb devices` 无设备，因此未安装升级、未执行设备仪器测试；真实软键盘、Android 安全区、系统返回和图片/分享流程待真机验证。此前真机证据不计作本轮界面验收。

## 2026-09-19 V1.1 真机升级与回归

- 版本更新为 npm `1.1.0`、Android `versionName 1.1` / `versionCode 2`，用户授权提交与覆盖安装。
- 从连接的 OPPO PGAM10 提取实际已安装 APK，核验证书后使用匹配的 legacy debug 密钥签名，经 `adb install -r` 成功覆盖升级。未卸载、未清空应用数据，仍为个人安装调试包，不是应用商店正式签名发布。
- 升级前保留应用私有文件备份、旧 APK 及业务 JSON 基线；升级后 SQLite 业务状态与基线一致。恢复材料和证据在忽略目录 `.android-tools/v1.1-acceptance/`，不提交私有数据及密钥。
- 真机首次回归发现冰箱弹窗偏移：生产构建将 `translate: none` 合并为 transform，但居中工具类的独立 translate 仍生效。在手机弹窗规则中清零两个位移变量修复，未改变业务逻辑或数据库结构。
- 补充生产预览弹窗边界回归（含 336px 压缩高度），手机端 E2E 通过；领域/同步单测 15 项通过。WebDAV 模拟 E2E 已更新菜谱入口并通过，包含首次同步、冲突及损坏数据保护。
- 重新完成 build → Capacitor sync → Android assembleDebug / testDebugUnitTest，重新签名覆盖安装。Android JVM 单测仍仅为模板测试，不作为业务验收证据。
- `tests/android_upgrade.py verify` 在真机 WebView 通过：新建菜谱写入 SQLite；按食材搜索、确认两份菜；键盘弹出时弹窗边界正常且可滚动确认；库存 30g 抵扣 200g 需求后缺口 170g；按日排餐、餐次三份、一周总览；force-stop 后持久化；编辑/删除不破坏历史餐次快照；设置入口可达。
- 验收完成后恢复全部原始业务状态，并核对规范化 JSON 摘要一致（报告 `restored: true`）。评审截图与 V1.1 APK 保留为交付证据；此前确认删除的 `.android-tools/e2e/backup.json` 未重建。
- 本轮未完成：真实 AI 账户请求、真实坚果云及双设备同步、图片选择/拍照、系统导出分享、全面系统返回/安全区与断网矩阵验证；不宣称全部功能均已验收。未改动用户原有 `tests/android-native.mjs`。


## 2026-09-20 同步确认弹窗与启动同步

- 手动检查后的版本选择改为居中独立弹窗，展示本地/云端概况及方向说明；有云端版本时始终提供“上传到云端”和“下载到本机”，无版本时说明原因。取消/系统返回不执行同步，同步中禁止关闭及重复操作。
- SyncPanel 作为应用级持久组件，在本地数据加载完成后、已有配置与凭据时，每次冷启动检查一次；设置页通过 portal 显示配置，进出设置不重复启动检查。启动自动同步默认开启，可关闭，不做后台常驻轮询。
- 基线记录绑定服务器与账号。可信基线下单端变化自动上传/下载；首次连接、账号切换、旧版无账号绑定基线或双方变化需确认。恢复历史/待发布副本后，下次启动不自动用云端最新版本覆盖。
- 检查/下载/保存恢复期间保护新增本地修改，恢复前保留备份；本机选菜和编辑草稿保留。网络失败提示并允许继续离线使用。
- 新增 tests/e2e_sync_startup.py（Vite 开发服务器，新启动实例运行）：覆盖冷启动上传/下载、首次连接、账号切换、冲突、关闭开关、503、不重复检查、下载期间新增库存、取消/系统返回、忙碌状态与三个视口居中。仅注入模拟凭据，不连接真实坚果云。
- 15 项领域/同步单测、既有双设备 WebDAV 回归（27 请求）、应用确认回归通过。前端 build、Capacitor sync 和 Android assembleDebug 通过。手机弹窗截图保留在 .android-tools/sync-dialog-review.png。
- 本次未安装到真机，也未使用真实账号验证启动同步；模拟测试和构建不代表真实网盘验收。

## 2026-09-20 验收阻断修复

- 验收报告及失败回归基线提交：`0a50c95`。后续修复公开链接 HTTPS 跳转与移动页面请求、AI 字段校验和异常草稿保护、备份嵌套字段及重复菜谱 ID 校验，保留无 ID 手动库存兼容。
- 新 APK 已安装 PGAM10；真实小红书分享链接 → 正文 → 当前文本 AI → 核对保存 → 重启保留通过。测试数据和偏好已恢复，日志继续采集。
- 29 项单元测试、原有 9 套浏览器回归、新增 9 个边界用例、Android 构建及真机核心/升级回归通过。系统栏改用 native 处理，新版冷启动未再出现安全区 CSS 注入异常。
- 真实视觉识别仍受供应商可用性阻断，不能宣称全量外部链路通过。详细范围、历史失败及证据见 `docs/E2E验收-2026-09-20.md`。
