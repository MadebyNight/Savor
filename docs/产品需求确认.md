# 食光 · 产品需求确认

v0.5 · 2026-09-18 · 已确认的 MVP 目标（不是完成声明）

## 产品与边界

- Android 系统返回先收起软键盘，再关闭最上层弹窗（确认框视为取消），然后返回设置/菜谱编辑的上一级；菜谱草稿保留。周菜单总览返回单日。五个底部导航页是一级页面，无弹窗或二级页面时保留系统默认离开行为。

Android 本地个人应用，Capacitor 封装现有高保真 React 原型，保留五个模块与米白暖黄视觉。核心功能离线可用，不建设账号、业务服务器、实时协作或后台常驻同步。交付可安装 APK。

V1.2.1 增加菜单预计营养与营养周回顾，不代表个人实际摄入；不增加个人热量录入、健康目标或饮食处方。成品重量可选，仅描述整菜规模。小红书链接获取优先复用开源方案，不再限定官方。评估指定链接正文/评论读取及 Android 本地运行条件；现有 MCP/Playwright 方案依赖外部浏览器与登录，不适合直接打包。未找到适用方案前保留粘贴文字、截图、手动录入，不自行重造复杂爬虫。

## 手机端界面（2026-09-18 确认）

- 正式 Android 界面采用已确认的手机原型方向：紧凑顶栏，点单、冰箱、菜篮子、周菜单、菜谱五个底部入口。
- 点单首屏直接展示分类与菜品；左侧分类常驻，右侧小图单列，支持菜名或食材搜索，图标缩小但加减点击区域保持 48px。
- 冰箱与采购采用左分类、右列表；库存采用紧凑行，点击打开底部编辑面板。
- 周菜单默认按日查看，支持切换日期与一周纵向总览；点餐次打开已确认菜品选择，不要求拖放。真实日期、快照、份数与历史复制规则不变。
- 菜谱入口先展示已有菜谱，新建或编辑进入独立表单；离开保留草稿。设置保留从点单顶栏进入。
- 桌面浏览器保留开发预览能力，不作为主要交付目标。

## 点单与采购规则

- 一份是完整一道菜，选菜和餐次用正整数份数，默认 1，不按人数换算。
- 未确认选菜与已确认采购独立；确认整体替换当前采购，允许确认空清单。
- 仅有一份全局当前采购清单，不随切周变化，不保存采购历史。
- 确认时保存菜谱快照；采购按快照与确认份数计算，库存变化更新缺口。
- 周菜单安排不影响采购、不受采购份数限制；移除确认菜品不移除已有安排。
- 同名同单位需求先合并，再抵扣未过期库存；去首尾空格、统一明确单位写法，不做比例换算或同义食材推断。
- 适量/数量未知的菜谱食材可保存，清单标记需自行确认，不参与数值抵扣。
- 提供分类、空状态、PNG、HTML .doc 导出、系统分享和文字复制。

## 菜谱与周菜单

- 菜谱新建、编辑、删除及图片上传；菜名、至少一种食材、一步做法必填，时间、重量、图片可选。
- 同名可保存并提醒；沿用现有分类，不增加分类管理。
- 编辑只影响未来确认和安排；删除移出菜谱库和未确认选菜，已有采购及菜单快照保留。
- 安排时复制菜谱快照，同餐重复加入同菜合并份数；可调整份数、移除、点击或拖放。
- 周一至周日，按本地日历日期存储，可编辑过去和未来，时区变化不移动已存日期。
- 编辑自动保存到目标周，展示保存状态；不再按保存当天另建存档。
- 历史就是各周菜单。复制到空周直接复制，到非空周确认后整体替换，不做合并，不联动采购库存。
- 清空只影响所选周，需确认，其他周和业务数据不变。
- 旧版无真实日期菜单作为旧存档保留，用户复制到指定周，不猜日期；缺失菜谱标记缺失。

## 冰箱与草稿

- 库存按批次记录，数量正数可小数；可改数量、单位、入库日期、保存期并删除。
- 过期批次不抵扣，不自动删除；期限未知正常抵扣但提示未知。入库日算第 1 天。
- 按所选食材找菜仍要求同时包含全部名称；按菜谱 1 份列出其余缺口。
- 首次安装有可编辑删除的示例菜谱，库存、点单、采购、菜单为空；升级不重加示例。
- 手动表单保留草稿或离开时提示放弃；AI 草稿持久保存，批量逐项编辑勾选确认，已保存项标记避免重复。

## AI

- 菜谱图文与正文统一到“导入菜谱”页面，冰箱拍照保持独立；设置只负责 AI 配置、备份恢复和坚果云同步，从识别进入设置后返回原任务。菜谱与食材草稿隔离；旧图文和正文草稿首次进入统一导入时合并，原件保留。
- 图片提供“相册选择”和“拍摄”独立入口；选图后预览并保留到本地草稿，取消选择不清空已有图片，确认发送后才请求 AI。

- 应用通过原生网络层直接请求服务商，配置接口、模型和 Key；优先 DeepSeek，协议及模型以官方核验结果为准，不承诺任意协议。
- 文字、菜谱图片、拍照、小票提取为菜谱或库存草稿，不直接改正式数据、不自动排餐。
- 仅有菜名的输入留待补充，不编造食材步骤；不默认估算重量、热量、保存期。
- 菜谱识别发送前保留简短操作确认；失败保留输入、允许手动重试和录入；取消停止等待，不能承诺撤销服务端计费。

## 存储、备份与同步

- AI 菜谱和食材识别结果使用独立核对窗口，确认保存后才入库；关闭保留草稿，支持重新打开和部分保存。失败和空结果独立提示，不把需要处理的结果放在设置按钮下方。

- AI 配置旁提供“测试连接”，使用当前填写的接口、模型和 Key（留空则使用已保存 Key），确认后发送固定短文本，不发送业务数据、不保存配置。成功显示请求模型、接口返回模型及耗时；未返回模型名称时明确说明，不据此推断底层模型或图片识别能力。失败保留配置并提示原因，允许停止等待。
- AI 地址支持纯域名、`/v1` 基础地址及完整对话接口；界面仅保留可编辑的接口地址，网页响应与 AI 正文 JSON 解析失败分别提示。
- 公开链接仅沿 HTTPS 跳转获取正文，不携带 AI 或网盘凭据；需要登录或无法提取时仍保留粘贴文字、截图入口。模型列表存在和文本测试成功均不能证明视觉能力；停服与网络超时明确提示，保留原文和草稿。
- AI 返回字段先校验，再进入核对窗口；兼容单个食材对象及文字步骤，不丢弃错误食材。已有异常草稿保留并提供说明。恢复备份前校验菜谱、库存、菜单快照的字段及菜谱 ID 唯一性，非法数据不能替换本地数据；兼容旧版无 ID 的手动库存。

- SQLite 保存业务数据，私有目录保存图片，Preferences 保存普通偏好；首次保存凭据即使用 Keystore 加密。
- 同步菜谱、图片、库存、确认采购、各周菜单；选菜与编辑/AI 草稿留本机。
- Key、WebDAV 密码不进入仓库、APK 固定配置、日志或普通备份/同步包。
- 完整备份可导入导出，恢复整体替换前备份当前数据，不做合并。原型数据通过显式导出/导入迁移。
- 坚果云支持手动同步和启动时自动同步（默认开启，可在设置关闭）；每次冷启动读取本地数据和已保存凭据后检查一次，不做后台常驻同步。首次连接、切换账号或缺少可信基线时弹窗确认；已有同账号基线时，单端变化自动沿对应方向上传或下载，双方变化弹窗选择，不静默覆盖。检查或下载期间产生本地修改时停止自动恢复，保留修改。手动确认弹窗明确展示本地与云端版本，有云端版本时提供上传和下载两个方向。
- 首次双方有数据需选择，一方为空明确方向；禁止空设备静默覆盖云端。
- 图片与数据完整校验后才应用；上传完成才发布版本，失败保持原数据。
- 保留最近 10 个成功同步版本，未解决冲突副本不自动清理。

## 验收

- 业务确认统一使用应用内弹窗，禁止使用 `window.confirm/alert/prompt`。删除、清空、覆盖、恢复保留明确影响说明和二次确认，取消/关闭不执行操作，默认焦点为取消。
- AI 对外发送继续单独征得确认，草稿覆盖不得隐式跳过；普通成功/错误反馈使用应用内轻提示。照片选择、拍照、文件保存、分享及权限授权保留系统界面。

核心流程在 APK 离线可运行、重启及升级保留数据；采购计算、快照、日期、备份恢复、AI 草稿和同步冲突需针对性测试。完成 Android 构建和端到端测试后才声明可用。真实凭据或设备等外部阻塞记录到开发进度，未验证部分不能标完成。

## V1.1.2 交互一致性

统一按钮触摸反馈、灰绿焦点与复选框、柔和语义提示、底部面板与居中弹窗动效；关闭按钮至少 48×48px，并尊重系统减少动画设置。业务与数据规则不变。修复标准与验证见 [V1.1.2交互修复](V1.1.2交互修复.md)。

### 开发者 AI 配置补充

V1.1.2 在 AI 配置下方提供密码解锁入口。用户明确选择受控 APK 内置加密配置、在手机本地解密，不采用服务端转发。整套 URL/模型/Key 使用 AES-GCM 加密；密码派生密钥，不在客户端硬编码解锁密码。解锁后采用独立 Keystore 槽保存，保持个人配置并支持关闭恢复；仍需确认后才发送识别内容。仅密文允许随此受控 APK 分发，真实 Key 不得进入源码、日志、测试或 Git。完整流程与安全边界见版本修复文档。

### GitHub 公开版

GitHub 仅公开源码与不含开发者密文的 APK，公开版隐藏开发者入口并使用个人 AI 配置。开发者版由用户自行分发。发布必须从干净 Git 工作区构建 release APK 并核对包内无共享凭据及密文。

## V1.2.1 已确认增量

- 界面按最终预览补齐：冰箱名称搜索与分类、期限组合筛选，空结果可清除筛选；周菜单显示目标周范围和当前视图菜品条目数（不是份数之和），可从顶部选择餐次安排。保留历史、保存期来源及真实营养说明，不修改采购与快照规则。

- 五餐按早餐、午餐、下午茶、晚餐、夜宵排列，保留旧三餐 key；单日表直接展示菜名与份数，菜单重复安排不改变采购。
- 保存期按保存条件和可靠来源估算，包装及手动值优先；未知允许入库且标明待补充。修改保存方式先提示重算。临期按总保存期 ≤3 / 4–7 / >7 天分别提前 1 / 2 / 3 天。
- 菜单预计营养按整菜快照和份数计算；本地可靠食材数据优先，用户确认后 AI 仅补缺；未知不能当零，离线可读，来源可辨。
- 周回顾覆盖目标周一至周日完整计划；统一星期/时间默认周日 20:00，应用内和系统通知分别开关、按前后台分流。跨周补看保留原周，查看回顾即去重，不以生成 AI 报告为条件。
- 完整验收以 `V1.2.1-TODO.md`、`V1.2.1-界面设计.md`、`V1.2.1-实施计划.md` 为准；实现已接入，尚需真机验收；以 TODO 的证据和未验证项为准。

V1.2.1 实现状态：FDA 冷藏参考、30 项 CoFID 离线营养、主动 AI 补充与周报、目标周持久化双渠道提醒均已接入。提醒初始关闭，使用非精确本地调度；营养报告进入 SQLite 业务状态与同步，提醒偏好仅本机保存。布局修复包已在 OPPO PGAM10 完成覆盖升级、当前系统字体/1.15 倍字号、键盘与返回验收；通知和相机的既有结果及其余未验项见《开发进度》和 V1.2.1 TODO，不以此次布局测试替代。


## 2026-09-22 营养回顾层级调整

- 设置导航保持单行，空间不足时横向滑动；切换后选中项进入可见区域。
- 周菜单提供独立营养回顾卡片入口，主页面突出下一周建议和默认展开的每日概览，周合计折叠展示。
- 日期始终对应当前回顾覆盖周的周一至周日，共 7 天；点击切换单日详情，窄屏横向滑动，不固定为上周、不截掉周末。下周建议对应目标周之后的一周。
- 保留“菜单预计营养，非实际摄入”和简短的部分估算状态；不将未知当零或据缺失数据判断营养不足。
- AI 建议经用户确认后生成，正文以简短回顾和 1–2 条可执行建议为主，不平铺全部指标及缺失清单。保存结果可离线查看，菜单变化时标记待更新，取消或失败保留旧建议。
- “高级计算”作为独立次级页面，集中承载重算、逐道克重和营养补充、缺失项、参考来源。返回保持所选日期，计算更新仍写入目标周快照。
- 本次不新增实际摄入记录、个人营养目标或饮食处方，不改采购计算和历史快照规则。


## 2026-09-22 冰箱、采购与周菜单调整

- 冰箱状态标签简化为“过期”或“剩余 N 天”，到期当天显示剩余 0 天；计算边界不变。
- 冰箱添加入口统一为拍摄、相册选择、手动添加三项，取消选择留在原页。以选图动作发起识别，不再二次询问发送；入口不展示发送机制和费用说明。菜谱图文/正文识别仍保留原发送确认。
- 识别结果仍须核对后入库；食材新结果追加到已有待保存草稿，读取或识别失败不丢旧草稿，离开页面或取消等待后忽略迟到结果；恢复页面不自动重发。
- 冰箱只显示“看看能做什么”入口，点击后在独立面板选择匹配菜品，再查看做法和所需食材；过期食材不用于推荐，匹配种类不代表数量已备齐。
- 采购清单逐项勾选已买，可取消，离线和重启保留并随业务备份/同步；仅标记进度，不自动入库。名称、单位或需求数量变化时原勾选失效，条目移除后清理勾选。
- 周菜单历史与清空本周在同一行，移除“菜单修改自动保存 · 安排不会增加采购量”；实际保存与采购规则不变。

### 页面文案精简（2026-09-23）

- 常规页面不展示技术实现、发送机制、费用、自动保存等说明，也不重复解释已有按钮。必要输入提示优先使用字段标签与占位符。
- 保留错误、空状态、数据不完整状态及删除/覆盖确认；营养计算来源与覆盖项数只在高级计算中查看。

### 菜谱导入入口合并（2026-09-23）

- 点击“导入菜谱”直接进入导入页，同页显示相册选择、拍摄及一个链接／正文输入框，取消图文／正文方式选择的中间层。
- 完整 HTTP(S) 链接或常见分享口令显示“获取正文”，提取后回填原输入框供核对识别；普通正文直接识别。失败保留输入，取消后迟到内容不覆盖新输入；仅允许 HTTPS 网页提取。
- 图片选择、移除与正文编辑保留在同一份菜谱草稿中。菜谱识别仍保留发送及覆盖草稿确认；核对保存后才入库。

### 点单密度与冰箱推荐（2026-09-23）

- 所有手机页面统一压缩顶栏与底部导航，保持标题、按钮和安全区规范一致；点单另行压缩搜索框及确认栏。菜品缩略图与名称、食材摘要并排紧凑排列，时长在详情查看，加减份数保留 48px 点击区。长名称允许换行，摘要省略部分可在详情查看。
- 冰箱推荐直接匹配整个菜谱库，不需要预先选菜；空状态为“菜谱库中暂无匹配菜品”。推荐卡显示“已有 N 种食材”，不表示数量已足够，详情继续展示缺少食材。
- 明确同义名称统一用于推荐、库存对照和采购扣减：番茄/西红柿、土豆/马铃薯、卷心菜/包菜/圆白菜/洋白菜、花菜/菜花、黄瓜/青瓜、西兰花/绿花椰菜、香菜/芫荽。不合并生熟、部位及不同品种，用户原始食材名称保持不变。

### 菜篮子与冰箱顶部压缩（2026-09-23）

- 冰箱顶部顺序统一为搜索与期限筛选组合框、横向分类、相册/拍摄/手动添加紧凑入口、食材列表。期限筛选仍用应用内选择器，当前状态显示在框右侧；分类紧接搜索下方。
- 批次数移至手机顶栏“冰箱”标题旁，移除独占行的宣传语。添加入口保留文字与 48px 高度，不用遮挡列表的悬浮按钮。
- 菜篮子条目压缩上下留白，勾选区、名称、数量对齐，购买状态保留；长名称与数量可换行，不改变采购或勾选规则。

### 菜谱页紧凑布局（2026-09-23）

- 菜谱数量并入手机顶部标题，顶栏右侧仅保留“导入菜谱”；搜索框压缩为 44px。统一导入页直接提供相册、拍摄、链接/正文输入与手动添加，手动添加延续已有编辑草稿。
- 列表采用小缩略图、完整菜名、分类与时长，减少条目间距与重复标题；长菜名允许换行，点击整行仍打开详情。

## V1.2.2 界面与筛选（2026-09-23 已确认）

统一顶栏、搜索区、底栏的浅灰绿底色；Logo、深色加粗标题、次要功能键区分层级。菜谱搜索框内增加分类及制作时长筛选，包含用户自定义分类；未知时长独立保留。长表单/详情/营养使用统一次级页面，短选择和确认保留紧凑弹窗。编辑、导入、设置不叠加一级底部导航，返回保留草稿和上下文。业务计算及数据结构不变。完整范围见 [V1.2.2 界面整合](V1.2.2-界面整合.md)。

### 菜篮子库存抵扣说明（2026-09-23）

- 不增加顶部提示框；食材条目显示“还需买”数量，下方显示总需求与冰箱可用库存，部分抵扣时标注“库存不足，补买差额”。
- 用量未知保留“待确认”，显示可用库存作为参考并提示核对所需用量；已买状态与分类保留。可用量沿用同义名称、相同单位、未过期批次口径，不进行单位比例换算。
- 总需求与可用量由采购计算临时派生，不进入业务存储；采购数量、快照和勾选失效规则不变。
## 2026-09-23 自定义菜品分类与点单确认栏

- 点单分类、菜谱筛选和编辑建议统一来源；支持新增、重命名、删除，空分类保留。原默认分类可管理，“全部”固定，“未分类”保留为归集入口。
- 分类栏独立上下滚动；“管理分类”位于最后一个分类之后，随列表滚动，以分隔线区分。切换分类不重置侧栏位置，右侧菜品回到顶部；新增后退出管理时新分类滚动到可见位置。
- 删除分类必须二次确认，展示受影响菜谱数量，可指定接收分类，默认未分类。当前菜谱和编辑草稿迁移，采购与历史菜单快照保持原样；名称去首尾空格、禁止空白/重复/“全部”，新名称最长20字。
- 分类进入本地业务状态与备份/同步；旧数据从默认分类和已有菜谱分类初始化，已删除默认分类不在重启后恢复。AI导入和编辑产生的新分类也纳入统一列表。
- 手机点单确认栏移除右侧4px留白，黄色按钮贴齐上下和右侧边缘，右侧圆角与外层统一为12px，保留48px操作高度。

## V1.2.3 本地交付（2026-09-23）

- 版本统一为 1.2.3 / versionCode 6；包含菜篮子库存说明、自定义菜品分类、分类栏滚动管理入口和点单确认按钮对齐，以及既有 V1.2.2 界面整合。
- 固定 debug 构建使用已有 legacy 密钥；构建前校验证书、构建后核验 APK。密码仅从忽略配置读取，缺失或证书不符时失败，不再依赖临时重签。
- 本地安装包为根目录 `食光-V1.2.3.apk`，附 SHA256 校验文件；未上传 GitHub 或其他平台。本包沿用既有本地开发者版，不替代公开分发构建。

### 点单分类数量展示（2026-09-23）

- 左侧分类栏只显示分类名称，不重复显示菜品数量；右侧当前分类标题继续显示匹配菜品数量。保留分类独立滚动、末尾管理入口及现有点击区域；长分类名称允许换行。
